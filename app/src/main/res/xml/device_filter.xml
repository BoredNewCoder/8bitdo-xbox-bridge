<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Matches ANY attached USB device so the app auto-launches regardless of which
         controller/headset VID:PID is configured (see DeviceConfig.kt / SettingsActivity) —
         the app's own runtime logic then checks against the configured pair and does
         nothing if it's not a match. Standard pattern for USB host apps whose exact
         target hardware isn't fixed at build time. -->
    <usb-device />
</resources>
